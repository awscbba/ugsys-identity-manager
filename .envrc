#!/bin/bash
eval "$(devbox generate direnv --print-envrc)"
export AWS_PROFILE=awscbba
export APP_ENV=local
export AWS_DEFAULT_REGION=us-east-1
export IDENTITY_API_URL=https://nrhybmgg3j.execute-api.us-east-1.amazonaws.com
